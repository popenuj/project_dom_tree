require 'parser'

describe Parser do
  let(:header) { }
  let(:div) { "<div id = 'bim'>" }
  let(:p) { "<p class='foo bar' id='baz' name='fozzie'>" }
  let(:img) { "<img src='http://www.example.com' title='funny things'>" }



  describe "#parse_tag" do
    it "returns a Tag struct" do
    end

    it "can parse header tags"

    it "can parse p tags"

    it "can parse img tags"

    it "can parse div tags"


  end

end
